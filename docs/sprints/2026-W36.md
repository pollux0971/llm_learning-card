# Sprint 2026-W36

## 階段
I1 · 內容管線

## 目標(一句話)

把 `01-data-layer` 的考題/狀態檔/設定檔 schema 與原子寫入做完(phase-2),這是 I1 唯一現在就 ready 的 phase,也是 02-ingest-pipeline 與 09-lint 後續 phase 的地基。

## 本週 phase

| Phase | 狀態(開始時) | 預估 | 變異門檻 | 備註 |
|---|---|---|---|---|
| 01-data-layer/phase-2 | ready → done(2026-09-03) | 中(2 天) | 嚴格 95% | 實際:20 個 scenario(展開後 31 個)。過程中兩輪退回:一次新 step 跟 04-scheduler 撞名、一次 packages/contracts 被漏出變異測試範圍(85%→補到 100%,已正式納入 stryker.config.json) |

## Carry over(上週未完成)

無(Wave 0 這週全部收完)。

## 這週卡住、你可以現在解除的 gate

- **03-llm-router/phase-2** 卡在契約待決表的「本機模型與硬體」——machine 上有裝 `ollama`(`/usr/local/bin/ollama`)但目前沒在跑、也還沒確認 `qwen2.5:14b`(`.env.example` 裡的預留值)有沒有真的 pull 下來。決定後 `/decide` 記一筆,03/phase-2 就會變 ready。這是 I1 與 I2 的關鍵路徑,建議優先解掉。

## 這週仍卡住、要等別人的

- `01-data-layer/phase-3` — **已解鎖**(phase-2 done),ready,還沒排進本週
- `02-ingest-pipeline/phase-2` — 等 01 phase-3 done、03 phase-2 done
- `09-lint/phase-2` — 依賴最多(01 三個 phase、03 phase-3、05 phase-3、I5 通過),本來就是後期的東西,不用管

## @manual 場景確認清單

無(01-data-layer/phase-2 沒有 @manual 場景)。

## 單獨執行檢查

- [x] `standalone.json` 補了 `01-data-layer-questions`(`validate-question` 子指令),`npm run standalone -- --only 01-data-layer-questions` 通過

## Retro(週五填)

- 哪個 phase 比預期難?
- 為什麼?
- 下週要改估法或拆法嗎?
- 有沒有發現契約缺東西?
- 這週有沒有改過 prompt?改了有跑 golden 嗎?
