# 09 · lint — 下一步

## 目前

| 欄位 | 值 |
|---|---|
| 已完成 | 無 |
| 進行中 | 無 |
| 下一個 | phase-1(Wave 0) |

## Gate

**phase-1**:無 gate。自帶最小驗證器,吃 `learning-broken` fixture。

**phase-2** 需要:
- [ ] 自身:phase-1 `done`
- [ ] 跨資料夾:01 三個 phase `done`、03 phase-3 `done`、05 phase-3 `done`
- [ ] 整合:**I5 通過**

## Gate 未滿足時

**phase-2 會等很久**——它的依賴最多,而且要等到 I5。這是正常的,lint 本來就是後期的東西。

phase-1 完成後有一件**現在就該做**的事:拿 lint 自己的最小驗證器與 01 的驗證器,
對 `learning-broken` 的每一張卡各跑一次,**比對結論**。

- 結論相同 → 很好,契約寫得夠清楚
- 結論不同 → **這是重要發現**。代表契約有歧義,兩個人讀出了不同意思。
  停下來 `/decide`,把契約講清楚,而不是隨便挑一邊。

這件事只有在有兩份獨立實作時才做得到,錯過就沒了。

## 完成後

phase-2 完成即 I6 的最後一塊。到這裡系統可以長期無人維護地跑下去。
