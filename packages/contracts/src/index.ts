/**
 * @learning/contracts — contracts/types.md 的可執行版本。
 *
 * 01-data-layer/phase-1 在這裡放 zod schema 與從 schema 推導的型別。
 * 其他功能整合後(I1 起)從這裡 import 型別;Wave 0 期間各功能只能 import 這個套件與自己的目錄
 * (npm run boundaries 會檢查)。
 *
 * 目前是空的:Wave 0 scaffold 只留入口,內容由 01 填。
 */
export {};
