# learning-minimal

合法的最小 learning/。三張真的資安卡、三份考題、一份原始素材、一個依賴鏈。

lint 對它應該回報 **0 個問題**、退出碼 0。任何檢查在這裡誤報,就是那個檢查太嚴。

`state/reviews.json` 是空的——這是「剛 ingest 完、還沒學過任何東西」的狀態。
需要有複習歷史的測試用 `../reviews/` 底下的檔案。
