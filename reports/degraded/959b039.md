# 退化路徑報告 · 959b039-dirty

報告模式,不執法(ADR-044)。這份報告回答一個問題:**哪些測試在跑的時候走進了「失敗了卻回一個看起來正常的值」的分支?** 走進去不代表錯——很多測試就是在測那條分支——但沒有任何一個測試有明說它要走(opt-in 機制尚不存在),所以下面的數字全部是「未標記」。這是基準;之後的指標是**未標記數只准降**。

| 欄位 | 值 |
|---|---|
| 量測的 commit | `959b039-dirty` |
| 產生時間 | 2026-09-04T20:33:17.273Z |
| 指令 | `(--in reports/degraded/.raw/2026-09-04T20-30-51-298Z)` |
| 測試檔 / 測試(只算實際執行的;skipped 沒有 afterEach,不進分母) | 89 / 1779 |
| 訊號目錄 / 有呼叫點 / 被觸發 | 30 / 30 / 28 |

## 1. 基準數字

| 指標 | 數字 | 說明 |
|---|---|---|
| **觸發了退化分支但沒有明示 opt-in 的測試數** | **152 / 1779** | 這是基準。opt-in 機制尚不存在,所以 = 所有觸發的測試 |
| 其中觸發 `swallow`(失敗 → 正常值)的 | 101 | 「測試綠但走錯路」最典型的來源 |
| 其中觸發 `default-path`(沒給 → 自選一條路)的 | 74 | 不知情地測了 stub / 預設路徑 |
| 有 opt-in 標記的測試數 | 0 | 機制尚不存在 |
| **跨擁有者觸發**(測試的資料夾 ≠ 訊號的資料夾) | **13** | 最值得看:測試自以為在測 A,卻走了 B 的退化分支(§4) |
| 測試之外觸發的紀錄(beforeAll / 檔案頂層 / afterAll) | 0 | 歸不到單一測試,列在 §6 |

## 2. 訊號目錄(每處一行:檔案:行 — 訊號名)

位置是從原始碼 grep `witness('…')` 反查的,不是寫死的。「觸發測試數」是有幾個測試至少觸發一次;「次數」是總共觸發幾次。

| 位置 | 訊號 | 種類 | 擁有者 | 觸發測試數 | 次數 | 失敗了什麼 → 回了什麼 |
|---|---|---|---|---|---|---|
| `packages/core/src/llm/router-gateway.ts:103` | `llm.fallback.cloud-failed` | swallow | 03-llm-router | 23 | 24 | 雲端呼叫失敗(逾時 / 截斷 / 5xx / NoModel)→ 改走閘道,回 provisional=true 的正常 LlmResult |
| `packages/core/src/llm/router-gateway.ts:104` | `llm.fallback.budget-exhausted` | swallow | 03-llm-router | 3 | 3 | 當日預算已達上限 → 不打雲端,改走閘道,回 provisional=true 的正常 LlmResult |
| `packages/core/src/llm/router-gateway.ts:206` | `llm.gateway-router.probe-local-swallowed` | swallow | 03-llm-router | 1 | 1 | GatewayLlmRouter.probeLocal() 丟錯(含沒設 GATEWAY_API_KEY)→ 回 { available: false, models: [] } |
| `packages/core/src/llm/router-gateway.ts:155` | `llm.gateway-router.spend-no-log-zero` | default-path | 03-llm-router | 0 | 0 | 沒給 logPath 也沒注入 spendReader → 今日花費一律當 0,預算分支永遠走不到 |
| `packages/core/src/llm/router-impl.ts:161` | `llm.router-impl.probe-local-swallowed` | swallow | 03-llm-router | 1 | 1 | LlmRouterImpl.probeLocal() 的 prober 丟錯 → 回 { available: false, models: [] } |
| `packages/core/src/llm/router-impl.ts:114` | `llm.router-impl.local-prober-default` | default-path | 03-llm-router | 53 | 78 | 沒注入 localProber → 用 alwaysUnavailable,本機永遠不可用,路由只剩雲端或丟錯 |
| `packages/core/src/llm/router.ts:188` | `llm.cloud.probe-online-swallowed` | swallow | 03-llm-router | 2 | 2 | CloudLlmRouter.probeOnline() 的 fetch 丟錯(含逾時 abort)→ 回 false(離線) |
| `packages/core/src/llm/adapters/gateway.ts:239` | `llm.gateway.probe.http-not-ok` | swallow | 03-llm-router | 2 | 5 | GET /gateway/models 非 2xx → 回 { available: false, models: [] } |
| `packages/core/src/llm/adapters/gateway.ts:248` | `llm.gateway.probe.bad-body` | swallow | 03-llm-router | 2 | 6 | /gateway/models 回的 body 不是 JSON 或沒有 models 物件 → 回 { available: false, models: [] } |
| `packages/core/src/llm/adapters/gateway.ts:252` | `llm.gateway.probe.threw` | swallow | 03-llm-router | 8 | 8 | 換 token 或 fetch 丟錯(401 / 連線被拒 / 逾時)→ 回 { available: false, models: [] } |
| `packages/core/src/llm/adapters/gateway.ts:269` | `llm.gateway.chat.401-retry` | swallow | 03-llm-router | 2 | 2 | POST /gateway/chat 回 401 → 作廢 token 重換一次再重打,第二次成功就當正常回應 |
| `packages/core/src/llm/spend.ts:150` | `llm.spend.log-unreadable-zero` | swallow | 03-llm-router | 1 | 1 | log.jsonl 讀不到(任何錯誤,不只 ENOENT)→ 今日花費當 { usd: 0, calls: 0 } |
| `packages/core/src/llm/spend.ts:170` | `llm.spend.bad-line-skipped` | swallow | 03-llm-router | 1 | 2 | log.jsonl 某一行不是 JSON → 跳過那一行,花費照算 |
| `packages/core/src/llm/spend.ts:76` | `llm.spend.tokens-missing-zero` | swallow | 03-llm-router | 6 | 9 | llm_call 事件缺 tokens_in / tokens_out(或不是有限數字)→ 那一筆算 0 元 |
| `packages/core/src/llm/spend.ts:123` | `llm.spend.env-invalid-default` | swallow | 03-llm-router | 2 | 4 | LLM_DAILY_CAP_USD / LLM_PRICE_*_PER_M 有設但不是非負數字 → 靜默改用預設值 |
| `packages/core/src/grading/grade-fill.ts:35` | `grading.fill.llm-failed-strict` | swallow | 05-grading | 4 | 5 | grade.fill.llm 呼叫丟錯 → 回 { pass: false, grader: "fallback-strict" } 的正常 GradeResult |
| `packages/core/src/grading/grade-apply.ts:197` | `grading.apply.invalid-response-retry` | swallow | 05-grading | 9 | 9 | grade.apply 第一次回應解析不出 verdict → 記 warning 再打一次 |
| `packages/core/src/grading/grade-apply.ts:203` | `grading.apply.unparsable-skipped` | swallow | 05-grading | 5 | 5 | grade.apply 兩次都解析不出 → 回 { pass: null, grader: "error" },本次審核略過 |
| `packages/core/src/ingest/questions.ts:137` | `ingest.questions.retry` | swallow | 02-ingest-pipeline | 10 | 10 | ingest.questions 截斷 / 逾時 / 網路錯 → 重打一次(截斷時加倍 maxTokens) |
| `packages/core/src/ingest/questions.ts:203` | `ingest.questions.card-failed-skipped` | swallow | 02-ingest-pipeline | 5 | 5 | 某張卡的考題產生失敗 → 收進 failures 清單,其餘卡照寫,管線繼續 |
| `packages/core/src/ingest/generate-cards.ts:60` | `ingest.cards.regenerate-retry` | swallow | 02-ingest-pipeline | 5 | 7 | 卡 body 超過 100 字 → 用 regenerate prompt 再生成一次(最多 MAX_ATTEMPTS) |
| `packages/core/src/ingest/ingest.ts:304` | `ingest.pipeline.children-failed-skipped` | swallow | 02-ingest-pipeline | 2 | 2 | 子卡產生整批失敗 → 記一筆 warning,當作沒有子卡繼續 |
| `packages/core/src/ingest/ingest.ts:340` | `ingest.pipeline.deps-failed-skipped` | swallow | 02-ingest-pipeline | 1 | 1 | 依賴圖分析失敗(非 GraphFileCorruptError)→ 記一筆 warning,order 當空陣列繼續 |
| `packages/core/src/ingest/ingest.ts:47`<br>`packages/core/src/ingest/ingest.ts:282` | `ingest.category-default-security` | default-path | 02-ingest-pipeline | 3 | 4 | 沒給 category 且路徑推不出 → 分類一律當 "security" |
| `packages/core/src/ingest/deps.ts:379` | `ingest.deps.cycle-llm-retry` | swallow | 02-ingest-pipeline | 12 | 12 | 模型回的依賴圖有環 → 帶著環的路徑再問模型一次 |
| `packages/core/src/ingest/deps.ts:387` | `ingest.deps.cycle-local-repair` | swallow | 02-ingest-pipeline | 8 | 20 | 重問之後仍有環 → 本地丟邊修成 DAG,以修過的圖繼續寫 order |
| `packages/core/src/session/build.ts:87` | `session.router-default-fake` | default-path | 11-review-cli | 4 | 4 | buildSession 沒給 router → 用 contracts/fixtures/llm 的 FakeLlmRouter(stub) |
| `packages/core/src/prompt-quality/golden-run.ts:102` | `prompt-quality.mode-default-fake` | default-path | 12-prompt-quality | 5 | 5 | runGolden 沒給 mode → 走 fake(重播 fixture),不是 live |
| `packages/core/src/prompt-quality/golden-run.ts:135` | `prompt-quality.router-default-fake` | default-path | 12-prompt-quality | 14 | 24 | runGoldenFake 沒給 router → 用內建 fixture 目錄的 FakeLlmRouter(stub) |
| `packages/core/src/prompt-quality/fake-llm.ts:66` | `prompt-quality.fake.attempt-fallback-first` | default-path | 12-prompt-quality | 0 | 0 | FakeLlmRouter 第 N 次呼叫沒有 attempt=N 的 fixture → 重播 attempt=1 的那一份 |

## 3. 測試 → 它觸發的退化分支

只列有觸發的測試(152 個);沒觸發的 1627 個不列。依測試檔分組,括號是「觸發 / 該檔測試數」。

### `packages/core/src/grading/grade-apply.test.ts` (8 / 32) · 擁有者 05-grading

- gradeApply > does not throw or write a log when no logPath/logAppender is given → `grading.apply.invalid-response-retry`, `grading.apply.unparsable-skipped`
- gradeApply > leaves the card untouched (grader error, pass null) when both attempts are unparseable → `grading.apply.invalid-response-retry`, `grading.apply.unparsable-skipped`
- gradeApply > leaves the card untouched when both attempts have a verdict count mismatch → `grading.apply.invalid-response-retry`, `grading.apply.unparsable-skipped`
- gradeApply > logs exactly one retry event when the first response is invalid and the second succeeds → `grading.apply.invalid-response-retry`
- gradeApply > logs exactly one retry event, not two, when both attempts are unparseable → `grading.apply.invalid-response-retry`, `grading.apply.unparsable-skipped`
- gradeApply > retries once when the verdict count does not match the rubric length, and uses the valid retry → `grading.apply.invalid-response-retry`
- gradeApply > retries once with the same prompt when the first response is not JSON, and uses the second → `grading.apply.invalid-response-retry`
- gradeApply > writes the retry warning to logPath (file) when given a path instead of an appender → `grading.apply.invalid-response-retry`

### `packages/core/src/grading/grade-fill.test.ts` (4 / 16) · 擁有者 05-grading

- gradeFillBlank > falls back to strict failure when no model is available → `grading.fill.llm-failed-strict`
- gradeFillQuestion > fails the whole question when only the third blank is wrong, naming it and its answer → `grading.fill.llm-failed-strict`
- gradeFillQuestion > lists only the first two accepted synonyms when a blank has three or more → `grading.fill.llm-failed-strict`
- gradeFillQuestion > reports the first wrong blank even if a later one is also wrong → `grading.fill.llm-failed-strict` ×2

### `packages/core/src/ingest/children.test.ts` (1 / 16) · 擁有者 02-ingest-pipeline

- generateChildrenForCards > records a per-child question failure without discarding the child card itself → `ingest.questions.card-failed-skipped`

### `packages/core/src/ingest/deps.test.ts` (11 / 53) · 擁有者 02-ingest-pipeline

- analyzeDependencies > describes the cycle path in the retry prompt, joined with " -> " → `ingest.deps.cycle-llm-retry`
- analyzeDependencies > when the model returns a cycle > calls the model a second time with the cycle described → `ingest.deps.cycle-llm-retry`
- analyzeDependencies > when the model returns a cycle > drops the offending edge and logs cycle_removed when the second attempt still cycles → `ingest.deps.cycle-llm-retry`, `ingest.deps.cycle-local-repair`
- analyzeDependencies > when the model returns a cycle > only drops the exact offending [from, to] pair, keeping edges that share just one endpoint with it → `ingest.deps.cycle-llm-retry`, `ingest.deps.cycle-local-repair`
- analyzeDependencies > when the model returns a cycle > passes the same computed maxTokens into both the first call and the cycle-retry call → `ingest.deps.cycle-llm-retry`
- analyzeDependencies > when the model returns a cycle > produces a graph with no cycle after dropping the offending edge → `ingest.deps.cycle-llm-retry`, `ingest.deps.cycle-local-repair`
- analyzeDependencies > when the model returns a cycle > reports no removed edge and does not log cycle_removed when the second attempt resolves the cycle → `ingest.deps.cycle-llm-retry`
- analyzeDependencies: local cycle repair loop > removes both edges when the retry response itself contains two independent cycles, and writes an order listing every card once → `ingest.deps.cycle-llm-retry`, `ingest.deps.cycle-local-repair` ×2
- analyzeDependencies: local cycle repair loop > removes the stale graph entry and order file left by a previous successful run when the local loop exhausts the drop limit, without touching another category → `ingest.deps.cycle-llm-retry`, `ingest.deps.cycle-local-repair` ×3
- analyzeDependencies: local cycle repair loop > writes neither deps.json nor the order file when the local loop still cycles at the card-count cap, and logs a warning naming the remaining cycle → `ingest.deps.cycle-llm-retry`, `ingest.deps.cycle-local-repair` ×3
- analyzeDependencies:殘留循環時碰到損壞的 deps.json > 記一筆 reason 為 graph file corrupt 的 warning(恰好一筆),丟 GraphFileCorruptError,檔案位元組不變 → `ingest.deps.cycle-llm-retry`, `ingest.deps.cycle-local-repair` ×3

### `packages/core/src/ingest/generate-cards.test.ts` (3 / 6) · 擁有者 02-ingest-pipeline

- generateCards > 多張候選卡各自獨立處理:正常的直接收,超長的照樣重試 → `ingest.cards.regenerate-retry`
- generateCards > 第一次超長、第二次符合上限:接受並記一次 regenerate → `ingest.cards.regenerate-retry`
- generateCards > 連續三次都超長:放進 parked,附三次嘗試紀錄,不接受 → `ingest.cards.regenerate-retry` ×2

### `packages/core/src/ingest/ingest.test.ts` (4 / 20) · 擁有者 02-ingest-pipeline

- runIngest > 字數重試後合格:記一筆 regenerate 事件,卡片正常建立 → `ingest.cards.regenerate-retry`
- runIngest > 省略 category 且路徑不是 3 段(raw/<file>):inferCategory() 判斷不出來,退回預設 security → `ingest.category-default-security`
- runIngest > 省略 category 且路徑不是以 raw/ 開頭(即使剛好 3 段):inferCategory() 判斷不出來,退回預設 security → `ingest.category-default-security`
- runIngest > 重試 3 次仍超過字數上限:卡片被 park,寫進 needs-review.json,並記警告事件 → `ingest.cards.regenerate-retry` ×2

### `packages/core/src/ingest/pipeline.test.ts` (9 / 16) · 擁有者 02-ingest-pipeline

- runIngestPipeline > a card truncated once and succeeding on retry does not end up in the final failure list → `ingest.questions.retry`
- runIngestPipeline > deps.json 損壞時不吞 GraphFileCorruptError,讓 CLI 以非 0 退出碼結束 → `ingest.deps.cycle-llm-retry`, `ingest.deps.cycle-local-repair` ×6
- runIngestPipeline > logs a warning naming the card id and the error for a level 0 card whose question generation fails → `ingest.questions.card-failed-skipped`
- runIngestPipeline > logs a warning naming the card id when a child card question generation fails → `ingest.questions.card-failed-skipped`
- runIngestPipeline > 單卡生成失敗不影響其他卡:一張 level 0 卡的考題生成失敗,其餘照常完成 → `ingest.questions.card-failed-skipped`
- runIngestPipeline > 整批依賴圖分析失敗:記警告後略過,level 0 卡、考題、子卡仍照常完成,depsOrder 與 cycleRemoved 給空值 → `ingest.pipeline.deps-failed-skipped`
- runIngestPipeline > 整批子卡生成失敗:記警告後略過,level 0 卡與考題仍照常完成,依賴圖只涵蓋 level 0 卡 → `ingest.pipeline.children-failed-skipped`
- runIngestPipeline > 沒有子卡時 message 不變:仍然只是 level 0 的張數(回歸) → `ingest.pipeline.children-failed-skipped`
- runIngestPipeline > 省略 category,rawRelPath 不是 3 段式(inferCategory() 判斷不出來):runIngestPipeline() 內部也退回預設 security,跟 runIngest() 一致 → `ingest.category-default-security` ×2

### `packages/core/src/ingest/questions.test.ts` (10 / 27) · 擁有者 02-ingest-pipeline

- generateQuestions retry on transient errors > calls onRetry with the reason when the first call is truncated and the retry succeeds → `ingest.questions.retry`
- generateQuestions retry on transient errors > doubles the token budget on the truncation retry and leaves the first call on the task default → `ingest.questions.retry`
- generateQuestions retry on transient errors > leaves the budget untouched on a network-layer retry → `ingest.questions.retry`
- generateQuestions retry on transient errors > leaves the budget untouched on a timeout retry → `ingest.questions.retry`
- generateQuestions retry on transient errors > retries once and succeeds when the first call throws a plain network-layer error → `ingest.questions.retry`
- generateQuestions retry on transient errors > retries once and succeeds when the first call throws LlmTimeoutError → `ingest.questions.retry`
- generateQuestions retry on transient errors > retries once and succeeds when the first call throws OutputTruncatedError → `ingest.questions.retry`
- generateQuestions retry on transient errors > throws the second error when both attempts throw a retryable error (only one retry, not a loop) → `ingest.questions.retry`
- generateQuestionsForCards > appends one llm_call event naming the card and the retry reason when a card is truncated once → `ingest.questions.retry`
- generateQuestionsForCards > records a failure for the third card by id and still writes the other four → `ingest.questions.card-failed-skipped`

### `packages/core/src/llm/adapters/gateway.test.ts` (13 / 69) · 擁有者 03-llm-router

- GatewayClient — 回應不是 JSON > probe 拿到 HTML 回不可用,不 throw → `llm.gateway.probe.bad-body`
- GatewayClient.chat > 401(token 過期)重換 token 後重試一次就成功 → `llm.gateway.chat.401-retry`
- GatewayClient.chat > 重試後還是 401 就丟 GatewayCallError,不無限迴圈 → `llm.gateway.chat.401-retry`
- GatewayClient.probe — 逾時是整段流程共用一份額度,不是每段各一份 > 換 token 4 秒 + models 2 秒(各自都沒超過 5 秒,加起來超過)→ 回不可用 → `llm.gateway.probe.threw`
- GatewayClient.probe — 逾時要涵蓋換 token 那一步 > 換 token 掛住不會留下逾時計時器 → `llm.gateway.probe.threw`
- GatewayClient.probe — 逾時要涵蓋換 token 那一步 > 換 token 永不回應時,probe 仍在逾時內回不可用,不會一直掛著 → `llm.gateway.probe.threw`
- GatewayClient.probe > /gateway/models 回錯誤狀態碼時回不可用,不 throw → `llm.gateway.probe.http-not-ok` ×4
- GatewayClient.probe > key 錯(401)回不可用,**不 throw** → `llm.gateway.probe.threw`
- GatewayClient.probe > probe 逾時會 abort 掉請求並回不可用,不會一直掛著 → `llm.gateway.probe.threw`
- GatewayClient.probe > token 換得到但 /gateway/models 連不上,一樣回不可用 → `llm.gateway.probe.threw`
- GatewayClient.probe > 回應形狀不對(models 不是物件)時回不可用 → `llm.gateway.probe.bad-body` ×5
- GatewayClient.probe > 狀態碼是錯的就不看 body,即使 body 裡有一份看起來正常的模型清單 → `llm.gateway.probe.http-not-ok`
- GatewayClient.probe > 連線被拒回不可用,不 throw → `llm.gateway.probe.threw`

### `packages/core/src/llm/router-gateway.test.ts` (44 / 107) · 擁有者 03-llm-router

- GATEWAY_FAILED 不從 router 的公開介面外洩 > deepen:完全離線時丟出來的不是 GATEWAY_FAILED → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GATEWAY_FAILED 不從 router 的公開介面外洩 > grade.apply:完全離線時丟出來的不是 GATEWAY_FAILED → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GATEWAY_FAILED 不從 router 的公開介面外洩 > ingest.cards:完全離線時丟出來的不是 GATEWAY_FAILED → `llm.router-impl.local-prober-default`
- GATEWAY_FAILED 不從 router 的公開介面外洩 > ingest.deps:完全離線時丟出來的不是 GATEWAY_FAILED → `llm.router-impl.local-prober-default`
- GATEWAY_FAILED 不從 router 的公開介面外洩 > ingest.questions:完全離線時丟出來的不是 GATEWAY_FAILED → `llm.router-impl.local-prober-default`
- GATEWAY_FAILED 不從 router 的公開介面外洩 > reteach.short:完全離線時丟出來的不是 GATEWAY_FAILED → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GATEWAY_FAILED 不從 router 的公開介面外洩 > 在線、雲端失敗、閘道也不通:備援兩邊都掛掉時仍不外洩 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter — probeLocal 的 catch 與檔案 log > 沒設 GATEWAY_API_KEY 時 probeLocal 回不可用,不 throw → `llm.gateway-router.probe-local-swallowed`
- GatewayLlmRouter.call — 一般路徑 > deepen 雲端沒事就走雲端 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 一般路徑 > grade.apply 雲端沒事就走雲端 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 一般路徑 > ingest.cards 走雲端 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 一般路徑 > ingest.deps 走雲端 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 一般路徑 > ingest.questions 走雲端 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 一般路徑 > reteach.short 雲端沒事就走雲端 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > deepen 丟契約 §7 的 NO_MODEL,不是閘道的錯誤碼 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > deepen 把閘道的原始錯誤留在 cause 裡,診斷資訊不丟掉 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > deepen 的 detail 字面就是「local gateway unreachable」 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > deepen 的訊息說得清「本機閘道不可達」,不是只有一句沒有模型 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > grade.apply 丟契約 §7 的 NO_MODEL,不是閘道的錯誤碼 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > grade.apply 把閘道的原始錯誤留在 cause 裡,診斷資訊不丟掉 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > grade.apply 的 detail 字面就是「local gateway unreachable」 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > grade.apply 的訊息說得清「本機閘道不可達」,不是只有一句沒有模型 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > ingest.cards 不變:離線一律 CLOUD_REQUIRED,而且閘道一次都沒被打 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > ingest.deps 不變:離線一律 CLOUD_REQUIRED,而且閘道一次都沒被打 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > ingest.questions 不變:離線一律 CLOUD_REQUIRED,而且閘道一次都沒被打 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > probeLocal() 在閘道整台不在時回不可用,不 throw(phase-2 的行為不變) → `llm.gateway.probe.threw`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > reteach.short 丟契約 §7 的 NO_MODEL,不是閘道的錯誤碼 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > reteach.short 把閘道的原始錯誤留在 cause 裡,診斷資訊不丟掉 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > reteach.short 的 detail 字面就是「local gateway unreachable」 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 完全離線(雲端不通 + 閘道也不通) > reteach.short 的訊息說得清「本機閘道不可達」,不是只有一句沒有模型 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 當日預算 > 差一點點還沒用完 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 當日預算 > 預算用完時 deepen 直接走閘道,連雲端都不打 → `llm.fallback.budget-exhausted`
- GatewayLlmRouter.call — 當日預算 > 預算用完的備援原因是 budget_exhausted → `llm.fallback.budget-exhausted`
- GatewayLlmRouter.call — 雲端失敗時的備援 > grade.apply 退到閘道並標 provisional → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 雲端失敗時的備援 > ingest.cards 不備援,丟 CLOUD_REQUIRED,而且閘道一次都沒被打 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 雲端失敗時的備援 > 備援那一筆 log 記下 fallback 與原因 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 雲端失敗時的備援 > 缺憑證這種設定錯誤不備援,原錯誤直接往外丟 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 雲端失敗時的備援 > 逾時也會備援 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 雲端整個連不上(probeOnline 回 false) > deepen 在 NoModelError 之後改走閘道並標 provisional → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.call — 雲端整個連不上(probeOnline 回 false) > grade.apply 與 reteach.short 也一樣 → `llm.router-impl.local-prober-default` ×2, `llm.fallback.cloud-failed` ×2
- GatewayLlmRouter.call — 雲端整個連不上(probeOnline 回 false) > ingest.cards 不因為閘道活著就改走它——契約 §7 的 CLOUD_REQUIRED 不變 → `llm.router-impl.local-prober-default`
- GatewayLlmRouter.call — 雲端整個連不上(probeOnline 回 false) > 備援那一筆 log 記下原因是 cloud_failed,而不是預算 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.callGateway — log 事件的組裝 > 備援那一筆記下 fallback_from 與原本的雲端錯誤訊息 → `llm.router-impl.local-prober-default`, `llm.fallback.cloud-failed`
- GatewayLlmRouter.callGateway — log 事件的組裝 > 預算用完的備援也有 fallback,但沒有 fallback_from(那一格沒有雲端錯誤) → `llm.fallback.budget-exhausted`

### `packages/core/src/llm/router-impl.test.ts` (3 / 12) · 擁有者 03-llm-router

- LlmRouterImpl.call — 路由決策後的組裝邏輯 > 決策是 cloud 時,轉呼叫底層 cloudRouter.call(),回傳它的結果 → `llm.router-impl.local-prober-default`
- LlmRouterImpl.probeLocal — 本機探測可注入,預設固定 unavailable(ADR-037) > 本機模型伺服器拒絕連線時,回報不可用,不把錯誤往外丟 → `llm.router-impl.probe-local-swallowed`
- LlmRouterImpl.probeLocal — 本機探測可注入,預設固定 unavailable(ADR-037) > 沒有注入探測器時,預設一律回報不可用(phase-2 沒有真的本機模型可偵測) → `llm.router-impl.local-prober-default`

### `packages/core/src/llm/router.test.ts` (2 / 41) · 擁有者 03-llm-router

- CloudLlmRouter.probeOnline / probeLocal > aborts the probe request once the probe timeout elapses → `llm.cloud.probe-online-swallowed`
- CloudLlmRouter.probeOnline / probeLocal > reports offline when the request fails → `llm.cloud.probe-online-swallowed`

### `packages/core/src/llm/spend.test.ts` (9 / 30) · 擁有者 03-llm-router

- computeDailySpend > 只有 tokens_out 沒有 tokens_in 也算得出來 → `llm.spend.tokens-missing-zero`
- computeDailySpend > 多筆事件相加 → `llm.spend.tokens-missing-zero` ×2
- computeDailySpend > 是純函式:不改動傳進來的事件陣列 → `llm.spend.tokens-missing-zero`
- computeDailySpend > 沒有 token 欄位的事件(逾時、截斷)算一筆但零元 → `llm.spend.tokens-missing-zero` ×2
- readDailySpend > log 檔不存在就是還沒花錢,不丟錯 → `llm.spend.log-unreadable-zero`
- readDailySpend > 壞掉的一行只跳過那一行,其他行照算 → `llm.spend.bad-line-skipped` ×2, `llm.spend.tokens-missing-zero` ×2
- readDailySpend > 讀 log.jsonl 算出當日花費 → `llm.spend.tokens-missing-zero`
- readSpendPrices / readDailyCapUsd > 不是數字就退回預設值,不丟錯 → `llm.spend.env-invalid-default` ×2
- readSpendPrices / readDailyCapUsd > 負數的價格退回預設值 → `llm.spend.env-invalid-default` ×2

### `packages/core/src/prompt-quality/cli.test.ts` (9 / 29) · 擁有者 12-prompt-quality

- cli main --live > 印出模型、token 進出,以及「不在價目表上就不估」的花費 → `llm.router-impl.local-prober-default` ×3
- cli main --live > 有結構性問題時逐項列出,結尾那句說清楚那不代表品質不好 → `llm.router-impl.local-prober-default` ×3
- cli main > --golden --fake 五組真任務加自我測試,一共 18 個輸入都跑得起來 → `prompt-quality.router-default-fake` ×6
- cli main > --golden --fake 沒指定 set 時,跑登記表裡的每一組,退出碼 0 → `prompt-quality.router-default-fake` ×6
- cli main > --golden --out 指到哪裡就寫到哪裡,不碰 repo 裡的 golden 目錄 → `prompt-quality.router-default-fake`
- cli main > --set 排在 argv 第一個時仍然只跑那一組 → `prompt-quality.router-default-fake`
- cli main > --task 是 --set 的舊名字,行為一樣 → `prompt-quality.router-default-fake`
- cli main > fake run 不印模型與花費那一行,結尾也不提結構性問題 → `prompt-quality.router-default-fake`
- cli main > 跑兩次 golden 再 diff,兩邊輸出都顯示出來 → `prompt-quality.router-default-fake`

### `packages/core/src/prompt-quality/golden-run.test.ts` (5 / 8) · 擁有者 12-prompt-quality

- runGolden > fake 模式下每次呼叫 router 都會觸發 onCall,不會碰網路 → `prompt-quality.mode-default-fake`, `prompt-quality.router-default-fake`
- runGolden > 在 run 目錄寫一份評分表,列出每個輸入、分數欄是空的,並列出兩個評分維度 → `prompt-quality.mode-default-fake`, `prompt-quality.router-default-fake`
- runGolden > 建立以日期命名的目錄,每個輸入一個輸出檔,並存一份當下的 prompt 檔 → `prompt-quality.mode-default-fake`, `prompt-quality.router-default-fake`
- runGolden > 記錄 model、provider、日期,以及 prompt 檔的 git commit → `prompt-quality.mode-default-fake`, `prompt-quality.router-default-fake`
- runGolden > 送進 router 的 prompt 一定包含 prompt 檔的內容,不只是把它快照起來 → `prompt-quality.mode-default-fake`, `prompt-quality.router-default-fake`

### `packages/core/src/prompt-quality/live-run.test.ts` (12 / 24) · 擁有者 12-prompt-quality

- runGolden --live:線上 > meta 記 token 合計,model 在價目表上時填美金粗估 → `llm.router-impl.local-prober-default` ×3
- runGolden --live:線上 > meta.json 落到磁碟上,內容跟回傳的一致 → `llm.router-impl.local-prober-default` ×3
- runGolden --live:線上 > model 不在價目表上時只記 token,不瞎猜金額 → `llm.router-impl.local-prober-default` ×3
- runGolden --live:線上 > 查不到價目時 estimated_cost_usd **欄位整個不存在**,不是一個 undefined 的值 → `llm.router-impl.local-prober-default` ×3
- runGolden --live:線上 > 查得到價目時欄位才存在 → `llm.router-impl.local-prober-default` ×3
- runGolden --live:線上 > 每次呼叫都被 router 記進 log(契約 §10 的 llm_call 事件) → `llm.router-impl.local-prober-default` ×3
- runGolden --live:線上 > 用設定的雲端模型跑,每個輸入一個輸出檔,meta.mode 是 live → `llm.router-impl.local-prober-default` ×3
- runGolden --live:線上 > 真的走到網路邊界:三個輸入 → 三次 /v1/messages → `llm.router-impl.local-prober-default` ×3
- runGolden --live:線上 > 輸出仍然跑結構性檢查(live 不是繞過檢查的後門) → `llm.router-impl.local-prober-default` ×3
- 錯誤與 meta 的欄位(審核補測) > fake 模式:golden set 沒有輸入時 model 與 provider 是 unknown → `prompt-quality.router-default-fake`
- 錯誤與 meta 的欄位(審核補測) > fake 模式:prompt 檔不見時丟錯,訊息指得出 task 與檔名 → `prompt-quality.router-default-fake`
- 錯誤與 meta 的欄位(審核補測) > meta.promptFileGitCommit 是真的短 sha,不是 uncommitted、也沒有換行 → `llm.router-impl.local-prober-default` ×3

### `packages/core/src/session/answer.test.ts` (1 / 10) · 擁有者 11-review-cli

- submitAnswer — grading error > leaves the card alone on disk and reports the error, but moves the session on → `grading.apply.invalid-response-retry`, `grading.apply.unparsable-skipped`

### `packages/core/src/session/build.test.ts` (4 / 6) · 擁有者 11-review-cli

- buildTodaySession > applies daily_cap from config/settings.yaml and reports the deferred count → `session.router-default-fake`
- buildTodaySession > does not write anything to disk while building the session → `session.router-default-fake`
- buildTodaySession > orders the queue by overdue ratio, matching what selectSession returns → `session.router-default-fake`
- buildTodaySession > reports nothing due when no card is due today → `session.router-default-fake`

## 4. 跨擁有者觸發

測試檔的擁有者(boundaries.owners.json)跟它觸發的訊號的擁有者不同。這些測試走進了**別的模組**的退化分支——它的斷言通常不是為那條分支寫的。

| 測試檔 | 測試 | 測試擁有者 | 走進了誰的退化分支 |
|---|---|---|---|
| `packages/core/src/prompt-quality/cli.test.ts` | cli main --live > 印出模型、token 進出,以及「不在價目表上就不估」的花費 | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/cli.test.ts` | cli main --live > 有結構性問題時逐項列出,結尾那句說清楚那不代表品質不好 | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/live-run.test.ts` | runGolden --live:線上 > meta 記 token 合計,model 在價目表上時填美金粗估 | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/live-run.test.ts` | runGolden --live:線上 > meta.json 落到磁碟上,內容跟回傳的一致 | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/live-run.test.ts` | runGolden --live:線上 > model 不在價目表上時只記 token,不瞎猜金額 | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/live-run.test.ts` | runGolden --live:線上 > 查不到價目時 estimated_cost_usd **欄位整個不存在**,不是一個 undefined 的值 | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/live-run.test.ts` | runGolden --live:線上 > 查得到價目時欄位才存在 | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/live-run.test.ts` | runGolden --live:線上 > 每次呼叫都被 router 記進 log(契約 §10 的 llm_call 事件) | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/live-run.test.ts` | runGolden --live:線上 > 用設定的雲端模型跑,每個輸入一個輸出檔,meta.mode 是 live | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/live-run.test.ts` | runGolden --live:線上 > 真的走到網路邊界:三個輸入 → 三次 /v1/messages | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/live-run.test.ts` | runGolden --live:線上 > 輸出仍然跑結構性檢查(live 不是繞過檢查的後門) | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/prompt-quality/live-run.test.ts` | 錯誤與 meta 的欄位(審核補測) > meta.promptFileGitCommit 是真的短 sha,不是 uncommitted、也沒有換行 | 12-prompt-quality | `llm.router-impl.local-prober-default` (03-llm-router) |
| `packages/core/src/session/answer.test.ts` | submitAnswer — grading error > leaves the card alone on disk and reports the error, but moves the session on | 11-review-cli | `grading.apply.invalid-response-retry` (05-grading)<br>`grading.apply.unparsable-skipped` (05-grading) |

## 5. 訊號 → 哪些測試檔碰到它

反向索引:改某個退化分支之前,先看哪些測試檔會受影響。

- `llm.fallback.cloud-failed`:`packages/core/src/llm/router-gateway.test.ts` (23)
- `llm.fallback.budget-exhausted`:`packages/core/src/llm/router-gateway.test.ts` (3)
- `llm.gateway-router.probe-local-swallowed`:`packages/core/src/llm/router-gateway.test.ts` (1)
- `llm.router-impl.probe-local-swallowed`:`packages/core/src/llm/router-impl.test.ts` (1)
- `llm.router-impl.local-prober-default`:`packages/core/src/llm/router-gateway.test.ts` (39), `packages/core/src/llm/router-impl.test.ts` (2), `packages/core/src/prompt-quality/cli.test.ts` (2), `packages/core/src/prompt-quality/live-run.test.ts` (10)
- `llm.cloud.probe-online-swallowed`:`packages/core/src/llm/router.test.ts` (2)
- `llm.gateway.probe.http-not-ok`:`packages/core/src/llm/adapters/gateway.test.ts` (2)
- `llm.gateway.probe.bad-body`:`packages/core/src/llm/adapters/gateway.test.ts` (2)
- `llm.gateway.probe.threw`:`packages/core/src/llm/adapters/gateway.test.ts` (7), `packages/core/src/llm/router-gateway.test.ts` (1)
- `llm.gateway.chat.401-retry`:`packages/core/src/llm/adapters/gateway.test.ts` (2)
- `llm.spend.log-unreadable-zero`:`packages/core/src/llm/spend.test.ts` (1)
- `llm.spend.bad-line-skipped`:`packages/core/src/llm/spend.test.ts` (1)
- `llm.spend.tokens-missing-zero`:`packages/core/src/llm/spend.test.ts` (6)
- `llm.spend.env-invalid-default`:`packages/core/src/llm/spend.test.ts` (2)
- `grading.fill.llm-failed-strict`:`packages/core/src/grading/grade-fill.test.ts` (4)
- `grading.apply.invalid-response-retry`:`packages/core/src/grading/grade-apply.test.ts` (8), `packages/core/src/session/answer.test.ts` (1)
- `grading.apply.unparsable-skipped`:`packages/core/src/grading/grade-apply.test.ts` (4), `packages/core/src/session/answer.test.ts` (1)
- `ingest.questions.retry`:`packages/core/src/ingest/pipeline.test.ts` (1), `packages/core/src/ingest/questions.test.ts` (9)
- `ingest.questions.card-failed-skipped`:`packages/core/src/ingest/children.test.ts` (1), `packages/core/src/ingest/pipeline.test.ts` (3), `packages/core/src/ingest/questions.test.ts` (1)
- `ingest.cards.regenerate-retry`:`packages/core/src/ingest/generate-cards.test.ts` (3), `packages/core/src/ingest/ingest.test.ts` (2)
- `ingest.pipeline.children-failed-skipped`:`packages/core/src/ingest/pipeline.test.ts` (2)
- `ingest.pipeline.deps-failed-skipped`:`packages/core/src/ingest/pipeline.test.ts` (1)
- `ingest.category-default-security`:`packages/core/src/ingest/ingest.test.ts` (2), `packages/core/src/ingest/pipeline.test.ts` (1)
- `ingest.deps.cycle-llm-retry`:`packages/core/src/ingest/deps.test.ts` (11), `packages/core/src/ingest/pipeline.test.ts` (1)
- `ingest.deps.cycle-local-repair`:`packages/core/src/ingest/deps.test.ts` (7), `packages/core/src/ingest/pipeline.test.ts` (1)
- `session.router-default-fake`:`packages/core/src/session/build.test.ts` (4)
- `prompt-quality.mode-default-fake`:`packages/core/src/prompt-quality/golden-run.test.ts` (5)
- `prompt-quality.router-default-fake`:`packages/core/src/prompt-quality/cli.test.ts` (7), `packages/core/src/prompt-quality/golden-run.test.ts` (5), `packages/core/src/prompt-quality/live-run.test.ts` (2)

**有呼叫點但沒有任何測試觸發**(2 個)——這些退化分支沒有測試走過,也就是沒有測試在保護它們:
- `llm.gateway-router.spend-no-log-zero`
- `prompt-quality.fake.attempt-fallback-first`

## 6. 測試之外觸發的

(沒有)

## 7. 掃描過但沒計數的分支

grep `catch` / `??` / `fallback` / `provisional` / `default` 掃到、讀過、判定不計數的,連同理由。列出來是為了讓掃描可以被審:覺得哪一條該計數,改 `scripts/degraded-report.ts` 的 `NOT_INSTRUMENTED` 與訊號目錄。

| 位置 | 理由 |
|---|---|
| `scripts/_root.ts:27` | 模板檔不手改:找不到 git root 就當 cwd,值得計數,回流模板 1.4.0 時一起做 |
| `scripts/check-doc-links.ts:107` | 模板檔不手改:skip 設定讀不到就當空設定 |
| `scripts/check-doc-links.ts:332` | 模板檔不手改:錨點解析失敗就當沒錨點 |
| `scripts/check-gherkin-dup.ts:82` | 模板檔不手改:.feature 讀不到就當空清單 |
| `scripts/check-phase-coverage.ts:194` | 模板檔不手改:FEATURE.md 讀不到就當沒有 phase 表 |
| `scripts/mutate.ts:153` | 鎖的持有者查不到時當「還活著」——方向是保守(不搶鎖),不是退化 |
| `packages/core/src/llm/adapters/gateway.ts:356` | 純設定預設值,沒有失敗發生 |
| `packages/core/src/llm/adapters/gateway.ts:358` | 純設定預設值,沒有失敗發生 |
| `packages/core/src/llm/adapters/gateway.ts:324` | 回應沒帶過期時間就用 50 分鐘——不改變路徑,只影響多久重換 token |
| `packages/core/src/llm/adapters/gateway.ts:151` | parse 不動回 undefined,但每個呼叫端都檢查了:token() 丟錯、probe() 走 bad-body 訊號,不會靜默 |
| `packages/core/src/llm/spend.ts:121` | 沒設環境變數用預設值是設定,不是失敗;有設但不合法那一行才計數(llm.spend.env-invalid-default) |
| `packages/core/src/llm/router.ts:30` | 沒給 logPath 就不寫 log:不改變執行路徑,只是少了副作用。測試斷言 log 內容的話會自己紅 |
| `packages/core/src/llm/router-impl.ts:112` | 純設定預設值 |
| `packages/core/src/ingest/ingest.ts:82` | CloudRequiredError 轉成明確的失敗結果與非 0 退出碼,不是靜默 |
| `packages/core/src/ingest/deps.ts:235` | 損壞的圖檔往外丟(ADR-041),不是靜默 |
| `packages/core/src/ingest/questions.ts:162` | 缺 fill/apply 補空陣列之後緊接著 validateQuestionFile(),數量不對會丟錯,不會靜默 |
| `packages/core/src/grading/grade-apply.ts:146` | parseApplyVerdict 回 null 是給呼叫端判斷用的訊號;呼叫端的兩個分支各自計數(grading.apply.*) |
| `packages/core/src/prompt-quality/structural-checks.ts:90` | parse 不動變成一條 issue 回報出去,是明確的失敗紀錄 |
| `packages/core/src/ingest/state.ts:60` | 清理暫存檔失敗不遮蔽原本的錯誤(ADR-040),不產生值 |
| `packages/core/src/schema/atomic-write.ts:53` | 同上,清理用 |
| `packages/core/src/lint/atomic-write.ts:64` | 同上,清理用 |
| `packages/core/src/grading/grade-fill.ts:56` | 作答數少於空格數時補空字串,下一層會判「沒有作答」,是形狀補齊 |
| `packages/core/src/ingest/generate-cards.ts:63` | regenerate 回應沒帶 examples 就沿用舊的——形狀補齊;regenerate 本身有計數(ingest.cards.regenerate-retry) |
| `packages/core/src/ingest/init.ts:43` | 空的 categories.yaml parse 成 null 補空陣列,形狀補齊 |
| `packages/core/src/lint/scan.ts:70` | 考題檔缺 card 欄位補空字串,後面的 lint 檢查會回報 |
| `packages/core/src/scheduler/select.ts:144` | 模擬工具的起始日預設值,不是產品路徑 |
| `apps/test-card/src/stubs/scheduler.ts:44` | Wave 0 stub:未知 stage 當 1 天。值得計數,但 apps/ 沒接 @contracts alias,等正式 scheduler 接上再看 |
| `apps/test-card/src/stubs/scheduler.ts:48` | 同上 |
| `apps/test-card/src/stubs/loader.ts:27` | Wave 0 stub 的設定預設值 |
| `apps/test-card/src/session.ts:138` | 索引越界補值,形狀補齊 |
| `packages/core/src/grading/fake-llm.ts:39` | 反例:fixture 找不到就丟錯,不退化 |
| `packages/core/src/ingest/fake-llm.ts:58` | 反例:離線時丟 CLOUD_REQUIRED,不退化 |
| `features/support/_router-guard.ts:41` | 反例:Background 該留下 router 卻沒有時丟錯,而不是就地生一個——這支檔案就是為了擋「安靜地變綠」而存在的 |

## 8. 怎麼重跑

```bash
npx tsx scripts/degraded-report.ts                      # 全部測試
npx tsx scripts/degraded-report.ts -- <某個 .test.ts>     # 只跑一個檔(反向驗證用)
npx tsx scripts/degraded-report.ts --in <raw 目錄>       # 只彙總,不跑
```

觀測點:`packages/contracts/src/witness.ts`(訊號目錄 + `witness()`)。hook:`scripts/degraded-witness.setup.ts`(只在設了 `DEGRADED_WITNESS_DIR` 時做事)。這份報告不改任何測試、不加任何標記;數字旁邊的 `.json` 是給下一次比「有沒有降」用的。
