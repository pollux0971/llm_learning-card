# apps/desktop

Tauri 2 桌面殼(10-desktop-shell)。

Wave 0 scaffold 只放 `package.json`;`src-tauri/`、`tauri.conf.json`、placeholder HTML 由
`10-desktop-shell/phase-1` 建立(`npx tauri init` 或手寫)。根目錄的 `npm run tauri dev`
會轉呼叫這裡的 `tauri dev`。
