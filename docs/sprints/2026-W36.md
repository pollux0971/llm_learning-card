# Sprint 2026-W36

## 階段
I1 · 內容管線

## 目標(一句話)

把 `01-data-layer` 的考題/狀態檔/設定檔 schema 與原子寫入做完(phase-2),這是 I1 唯一現在就 ready 的 phase,也是 02-ingest-pipeline 與 09-lint 後續 phase 的地基。

## 本週 phase

| Phase | 狀態(開始時) | 預估 | 變異門檻 | 備註 |
|---|---|---|---|---|
| 01-data-layer/phase-2 | ready → in-progress | 中(2 天) | 嚴格 95% | 16 個 scenario,無 @manual;有 phase-1 的 atomic-write.ts 可直接重用 |

## Carry over(上週未完成)

無(Wave 0 這週全部收完)。

## 這週卡住、你可以現在解除的 gate

- **03-llm-router/phase-2** 卡在契約待決表的「本機模型與硬體」——machine 上有裝 `ollama`(`/usr/local/bin/ollama`)但目前沒在跑、也還沒確認 `qwen2.5:14b`(`.env.example` 裡的預留值)有沒有真的 pull 下來。決定後 `/decide` 記一筆,03/phase-2 就會變 ready。這是 I1 與 I2 的關鍵路徑,建議優先解掉。

## 這週仍卡住、要等別人的

- `01-data-layer/phase-3` — 等 phase-2 done(本週如果 01/phase-2 收完,下週就 ready)
- `02-ingest-pipeline/phase-2` — 等 01 phase-2 **與** phase-3 done、03 phase-2 done
- `09-lint/phase-2` — 依賴最多(01 三個 phase、03 phase-3、05 phase-3、I5 通過),本來就是後期的東西,不用管

## @manual 場景確認清單

無(01-data-layer/phase-2 沒有 @manual 場景)。

## 單獨執行檢查

- [ ] `standalone.json` 目前只有 phase-1 的項目;phase-2 完成後記得補一個 CLI 指令進去(例如 questions/state 的驗證入口),讓 `npm run standalone` 涵蓋到它

## Retro(週五填)

- 哪個 phase 比預期難?
- 為什麼?
- 下週要改估法或拆法嗎?
- 有沒有發現契約缺東西?
- 這週有沒有改過 prompt?改了有跑 golden 嗎?
